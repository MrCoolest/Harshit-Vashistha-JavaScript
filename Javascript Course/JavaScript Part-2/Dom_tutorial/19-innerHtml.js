// innerHtml
const headline = document.querySelector('.headline');

// console.log(headline.innerHTML);
headline.innerHTML = '<h1>Inner html Changed from using jS';
headline.innerHTML += "<button class='btn'> Learn More </button>"
console.log(headline.innerHTML);
